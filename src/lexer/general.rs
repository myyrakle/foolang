#[derive(Clone, Debug, PartialEq)]
pub enum GeneralToken {
    // general syntax
    Arrow,            // ->
    Comma,            // ,
    SemiColon,        // ;
    LeftParentheses,  // (
    RightParentheses, // )
}
